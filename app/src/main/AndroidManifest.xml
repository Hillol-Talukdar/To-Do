<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".CustomApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDo"
        tools:targetApi="31">
        <activity
            android:name=".ui.screen.splashScreen.SplashScreenActivity"
            android:exported="true"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/Theme.ToDo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.screen.toDoDetailsScreen.ToDoDetailsScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_to_do_details_screen"
            android:theme="@style/Theme.ToDo" />
        <activity
            android:name=".ui.screen.toDoItemScreen.ToDoItemScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_to_do_screen"
            android:theme="@style/Theme.ToDo" />
        <activity
            android:name=".ui.screen.toDoListScreen.ToDoListScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_to_do_list_screen"
            android:theme="@style/Theme.ToDo" />
    </application>

</manifest>